import { BitsToKbsPipe } from './bits-to-kbs.pipe';

describe('BitsToKbsPipe', () => {
  it('create an instance', () => {
    const pipe = new BitsToKbsPipe();
    expect(pipe).toBeTruthy();
  });
});
